// Vale pro app do garçom inteiro: a lista de mesas, a mesa e a comanda.
// Sem isto o garçom toca na mesa e o celular fica parado até o servidor responder.
import { EsqueletoApp } from "@/components/esqueleto";

export default function Loading() {
  return <EsqueletoApp n={9} />;
}
